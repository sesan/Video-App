import React from 'react'

const VideoListItem = ({video}) => {//{video} is just like your props a new es6 format by making it a variable which is also similar to the code below
    // const video = props.video;

    return <li>Video</li>
}

export default VideoListItem